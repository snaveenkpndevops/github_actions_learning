name: test config var

on:
  workflow_dispatch:
    inputs:
      environment:
        type: choice
        options:
          - dev
          - prod

jobs:
  test:
    runs-on: ubuntu-latest

    env:
      PROJECT: ${{ vars.PROJECT_NAME }}

    steps:
      - name: Print vars
        run: echo "Project: $PROJECT"
