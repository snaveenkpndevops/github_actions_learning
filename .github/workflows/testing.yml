name: testing variables

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    env:
      PROJECT: ${{ vars.PROJECT_NAME }}

    steps:
      - name: Print vars
        run: echo "Project: $PROJECT"